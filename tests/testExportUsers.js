describe('Exporting User Search Results Via API', function () {
    // Omitting these tests for the current task at hand
    it('Customer kicks off job to export a list of users in CSV format', function() {
        return true;
    });

    it('Customer kicks off job to export a list of users in JSON format', function() {
        return true;
    });
});